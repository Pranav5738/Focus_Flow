# Backend (FastAPI) environment example

# Set to production on your hosting platform
APP_ENV=production

# REQUIRED in production: generate a long random string
# Example (do not use this value): JWT_SECRET=change-me
JWT_SECRET=

# Comma-separated list of allowed frontend origins.
# Use your real frontend domains. Domain names are case-insensitive; you can use lowercase.
CORS_ORIGINS=https://pranavS.store,https://www.pranavS.store

# === Persistence (recommended for production) ===
# MongoDB connection string (MongoDB Atlas recommended)
# MONGO_URL=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGO_URL=

# Optional database name
DB_NAME=habit_tracker

# === Fallback persistence (not recommended for multi-instance) ===
# If you do not use MongoDB, the backend falls back to a local file DB.
# DATA_FILE=/data/db.json
# DATA_FILE=

# === Leaderboard (optional) ===
# LEADERBOARD_TZ=UTC
# LEADERBOARD_INTERNAL_TOKEN=
