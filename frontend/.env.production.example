# Frontend (React) production env example

# IMPORTANT: must be set at BUILD time on your hosting platform.
# Point this to your deployed backend base URL (no trailing /api needed).
REACT_APP_BACKEND_URL=https://api.pranavs.store
